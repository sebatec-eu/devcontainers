name: "Build and push images"
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build-and-push:
    if: github.repository == 'sebatec-eu/devcontainers'
    strategy:
      fail-fast: false
      matrix:
        subFolders:
          - subFolder: hugo
            tags:
              - "latest"
              - "1"
              - "1.1"
              - "1.1.0"
          - subFolder: go
            tags:
              - "latest"
              - "1"
              - "1.0"
              - "1.0.0"
    concurrency:
      group: ${{ github.workflow }}-${{ toJSON(matrix.subFolders) }}-${{ github.ref }}
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Checkout (GitHub)
        uses: actions/checkout@v4
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Get Git commit timestamps
        run: echo "SOURCE_DATE_EPOCH=$(git log -1 --pretty=%ct -- ${{ matrix.subFolders.subFolder }})" >> $GITHUB_ENV
      - name: Build and run dev container task
        uses: devcontainers/ci@v0.3
        with:
          imageName: ghcr.io/sebatec-eu/${{ matrix.subFolders.subFolder }}-devcontainer
          push: always
          subFolder: ${{ matrix.subFolders.subFolder }}
          imageTag: ${{ join(matrix.subFolders.tags, ',') }}
        env:
          SOURCE_DATE_EPOCH: ${{ env.SOURCE_DATE_EPOCH }}
